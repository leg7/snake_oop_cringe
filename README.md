# Compiler

Pour compiler sans pas besoin d'installer quoi que ce soit juste lancer `./gradlew run --args [server|client]`

Alternativement pour utiliser le flake nix `nix develop --extra-experimental-features flakes --extra-experimental-features nix-command`
